# MongoDB credentials
MONGO_USER=jailbreak_admin
MONGO_PASS=jailbreak_secret_2024

# Ollama host (use "host.docker.internal" when running in Docker on Windows/Mac,
# or the actual IP of your Ollama server)
OLLAMA_HOST=host.docker.internal
